# shared — Cross-stage Reference (Layer 3)

> Reference material shared by **all** pipeline stages (stable across runs). One of
> the three Layer 3 locations in MWP (`references/`, `_config/`, `shared/`).

| File | Purpose |
|------|---------|
| [`glossary.md`](glossary.md) | Device → doc-folder map, service ports, and key terms used across stages |

For factory authoring rules see [`../_config/`](../_config/); for product reference see [`../src/docs/`](../src/docs/).
